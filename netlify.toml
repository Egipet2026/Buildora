# Netlify detects Next.js and installs its OpenNext adapter automatically —
# server actions run as functions, middleware as edge functions. Nothing else
# needs configuring here; the adapter chooses the publish directory itself.
[build]
  command = "npm run build"

[build.environment]
  NODE_VERSION = "22"
